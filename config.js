module.exports = {
    // the token of your discord bot
    botToken: '',
    // discord channel that the messages will be posted in
    textChannel: '',
    // this is required for discord
    userAgent: 'DiscordBot (oengus marathon fetcher for YOUR ORGANISATION HERE)',
    // the port that the application runs on
    port: 3000,
};
